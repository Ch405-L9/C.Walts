repos:
  - repo: local
    hooks:
      - id: gitleaks
        name: gitleaks staged secret scan
        entry: .venv/bin/python scripts/run_gitleaks_precommit.py
        language: system
        pass_filenames: false
        stages: [pre-commit]
