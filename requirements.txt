# A5 APPROVED: pip + project-local venv. Pins are exact where the audit measured
# a working version on this host; ranges only where the package is stable.
#
# Reproduce with:  python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
# Freeze with:     .venv/bin/pip freeze > requirements.lock

chromadb==1.5.8          # measured working against the existing 768-d stores
mcp==1.28.1              # security remediation: PYSEC-2026-3481/3482/3483
numpy==2.5.1             # exact-cosine oracle for Gate 1.2 determinism probes
ollama==0.6.1            # HARD dep: Chroma's OllamaEmbeddingFunction calls it
tiktoken==0.12.0         # A4 default — cl100k_base, verified offline-capable
rank-bm25==0.2.2         # PINNED: no release since 2022-02-16 (see audit R15)
pydantic>=2.12,<3
PyYAML>=6.0.3,<7
requests>=2.33,<3

# Loaders
pdfplumber>=0.11.9

# Dev / test
pytest>=9.0
ruff>=0.6
